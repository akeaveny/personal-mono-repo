dbt_personal_finance:
  target: sqlite
  outputs:
    sqlite:
      type: sqlite
      threads: 1
      database: personal_finance
      schema: main
      schemas_and_paths:
        main: ../../../personal_finance.db
      schema_directory: target
